const dialog=document.querySelector('#taskDialog'),input=document.querySelector('#taskInput'),tasks=document.querySelector('#tasks');
document.querySelector('#newTask').addEventListener('click',()=>dialog.showModal());
document.querySelector('#addTask').addEventListener('click',e=>{if(!input.value.trim()){e.preventDefault();return}const a=document.createElement('article');a.className='task';a.innerHTML=`<button class="check"></button><div><strong>${input.value.replace(/[<>]/g,'')}</strong><p>Inbox · New task</p></div><time>Today</time>`;tasks.prepend(a);input.value=''});
document.addEventListener('click',e=>{if(e.target.classList.contains('check')){e.target.classList.toggle('done');e.target.closest('.task').classList.toggle('completed');e.target.textContent=e.target.classList.contains('done')?'✓':''}if(e.target.matches('nav [data-view]')){document.querySelectorAll('nav [data-view]').forEach(x=>x.classList.remove('active'));e.target.classList.add('active');document.querySelector('#viewTitle').textContent=e.target.dataset.view}});
document.querySelector('#search').addEventListener('click',()=>{const q=prompt('Search tasks');if(q){[...document.querySelectorAll('.task')].forEach(t=>t.style.display=t.textContent.toLowerCase().includes(q.toLowerCase())?'grid':'none')}})
